os:
  - linux
services:
  - docker
sudo: required
addons:
  chrome: stable

language: node_js
node_js:
  - "10.15.3"

branches:
  only:
    - /.*/

before_script:
  - npm install -g @angular/cli

script:
  - ng build --prod --aot
  - ng test
